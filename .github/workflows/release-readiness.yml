name: Release readiness

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    name: Validate (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - windows-latest
    steps:
      - name: Check out dispatched commit
        uses: actions/checkout@v5
        with:
          ref: ${{ github.sha }}
      - name: Set up Node.js 22
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Run complete test suite
        run: npm test
      - name: Test packed package
        run: npm run package:smoke

  package:
    name: Create verified canonical artifact
    needs: validate
    runs-on: ubuntu-latest
    steps:
      - name: Check out dispatched commit
        uses: actions/checkout@v5
        with:
          ref: ${{ github.sha }}
      - name: Set up Node.js 22
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Build package
        run: npm run build
      - name: Read validated release metadata
        id: metadata
        run: npm run release:metadata
      - name: Create canonical package
        run: mkdir release-artifact && npm run release:package -- --output release-artifact
      - name: Verify canonical package
        run: npm run release:verify -- --artifact "${{ github.workspace }}/release-artifact/${{ steps.metadata.outputs.tarball }}" --checksum "${{ github.workspace }}/release-artifact/${{ steps.metadata.outputs.sidecar }}"
      - name: Upload commit-specific release candidate
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.metadata.outputs.artifact-base }}-${{ github.sha }}
          path: |
            release-artifact/${{ steps.metadata.outputs.tarball }}
            release-artifact/${{ steps.metadata.outputs.sidecar }}
          if-no-files-found: error
          retention-days: 7
